{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "if npx expo lint; then exit 0; else printf '{\"continue\":false,\"stopReason\":\"Expo lint failed — fix errors before committing.\"}'; exit 1; fi",
            "timeout": 60,
            "statusMessage": "Running expo lint before commit..."
          }
        ]
      }
    ]
  }
}
